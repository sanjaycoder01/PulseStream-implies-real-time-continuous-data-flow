# TODO: multi-stage production image for dashboard
# Placeholder only — do not implement image build yet.
FROM node:20-alpine AS base
WORKDIR /app
# TODO: copy workspace deps, build, and set CMD
CMD ["echo", "dashboard image not configured yet"]
