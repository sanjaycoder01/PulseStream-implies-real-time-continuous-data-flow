# TODO: multi-stage production image for raw-archiver
# Placeholder only — do not implement image build yet.
FROM node:20-alpine AS base
WORKDIR /app
# TODO: copy workspace deps, build, and set CMD
CMD ["echo", "raw-archiver image not configured yet"]
